export interface CanvasSize {
  /** Base logical width of the game canvas (used for aspect ratio) */
  width: number;
  /** Base logical height of the game canvas (used for aspect ratio) */
  height: number;
}

export default CanvasSize;

